const add = (a, b) => {
    return a+b;
}
const subtract = (a, b) => {
    return a-b;
}
module.exports = {
    // add: add,
    // subtract: subtract
    // 이름이 같을 때는 변수명만 써도 된다.
    add,
    subtract
};